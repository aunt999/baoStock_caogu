# 量化交易完整指南

## 一、环境配置

### 已安装组件

| 组件 | 版本 | 路径 |
|------|------|------|
| Python | 3.11.9 | F:\FilesData\QUANTAXIS\venv |
| QUANTAXIS | 1.10.19 | ✅ 核心模块可用 |
| Backtrader | 1.9.78.123 | ✅ 回测框架 |
| Akshare | 1.18.59 | ✅ 数据获取 |
| MongoDB | - | ❌ 需手动安装 |

### 启动方式

```powershell
# 方式1: 启动脚本
F:\FilesData\QUANTAXIS\启动QUANTAXIS.bat

# 方式2: 直接运行Python
F:\FilesData\QUANTAXIS\venv\Scripts\python.exe
```

---

## 二、数据获取

### Akshare 常用接口

```python
import akshare as ak

# 获取A股实时行情
stocks = ak.stock_zh_a_spot_em()

# 获取日K线
df = ak.stock_zh_a_hist(
    symbol="002285",       # 股票代码
    period="daily",        # daily/weekly/monthly
    start_date="20260101",
    end_date="20260430",
    adjust="qfq"           # 前复权
)

# 获取财务数据
report = ak.stock_financial_abstract(symbol="002285")

# 获取行业板块
sectors = ak.stock_board_industry_name_em()
```

---

## 三、策略回测（Backtrader）

### 策略模板

```python
import backtrader as bt

class MyStrategy(bt.Strategy):
    params = (
        ('period', 20),
    )
    
    def __init__(self):
        self.ma = bt.indicators.SMA(self.data.close, period=self.params.period)
        
    def next(self):
        if not self.position:
            if self.data.close > self.ma:
                self.buy()
        else:
            if self.data.close < self.ma:
                self.sell()
```

### 回测流程

```python
cerebro = bt.Cerebro()
cerebro.addstrategy(MyStrategy)
cerebro.adddata(data)
cerebro.broker.setcash(100000)
results = cerebro.run()
```

### 运行演示

```powershell
# 本地模拟数据演示
F:\FilesData\QUANTAXIS\venv\Scripts\python.exe F:\FilesData\QUANTAXIS\strategy_demo_local.py

# 真实数据回测（需网络）
F:\FilesData\QUANTAXIS\venv\Scripts\python.exe F:\FilesData\QUANTAXIS\strategy_demo.py
```

---

## 四、常用策略

### 1. 双均线交叉
- 快线上穿慢线 → 买入
- 快线下穿慢线 → 卖出

### 2. 布林带策略
- 价格触及下轨 → 买入
- 价格触及上轨 → 卖出

### 3. MACD策略
- MACD金叉 → 买入
- MACD死叉 → 卖出

### 4. RSI策略
- RSI < 30 → 超卖，买入
- RSI > 70 → 超买，卖出

---

## 五、风险控制

### 止损止盈

```python
class StrategyWithStop(bt.Strategy):
    def notify_order(self, order):
        if order.status == order.Completed:
            if order.isbuy():
                # 设置止损止盈订单
                self.sell(exectype=bt.Order.Stop, 
                         price=order.executed.price * 0.95)  # 5%止损
                self.sell(exectype=bt.Order.Limit,
                         price=order.executed.price * 1.10)  # 10%止盈
```

### 仓位管理

```python
# 固定金额
self.order = self.buy(size=1000)

# 按资金比例
size = (self.broker.getcash() * 0.1) / self.data.close[0]
self.order = self.buy(size=int(size))
```

---

## 六、文件清单

```
F:\FilesData\QUANTAXIS\
├── 启动QUANTAXIS.bat          # 启动Python环境
├── 使用指南.md                # 本文档
├── MongoDB安装指南.md          # MongoDB安装说明
├── strategy_demo.py           # 真实数据回测
├── strategy_demo_local.py     # 模拟数据回测
├── demo_simple.py             # Akshare示例
└── venv\                      # Python虚拟环境
```

---

## 七、下一步建议

1. **学习 Backtrader 文档**
   https://www.backtrader.com/docu/

2. **安装 MongoDB** 获得完整 QUANTAXIS 功能

3. **实盘交易** 考虑 VNPY 框架
